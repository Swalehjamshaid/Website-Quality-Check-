# Nixpacks.toml
[phases.setup]
nixPkgs = ["python311", "gcc", "gnumake"]
aptPkgs = [
  "libpango-1.0-0", "libpangoft2-1.0-0", "libcairo2-dev",
  "libgirepository1.0-dev", "libjpeg-dev", "libxml2-dev", "libxslt1-dev"
]

[start]
cmd = "gunicorn wsgi:application --bind 0.0.0.0:$PORT"
