services:
  - type: web # Your main application service
    # ... other configuration ...
    envVars:
      - key: CELERY_RESULT_BACKEND
        fromService:
          type: keyValue
          name: celery-broker
  # <--- CRITICAL: If you have more services, they would go here.
  # If this is your ONLY service, the 'services' key is closed AFTER this line.

resources: # <--- MUST be at the same level as the 'services:' key (Column 1)
  - name: celery-broker
    type: keyValue
    # ... rest of the resource block
