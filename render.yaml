# render.yaml — Docker-based web service (Blueprint)
services:
  - type: web
    name: website-quality-check
    runtime: docker
    region: oregon
    plan: free

    # Repo & branch
    repo: https://github.com/Swalehjamshaid/Website-Quality-Check.git
    branch: main

    # Build from your Dockerfile at the repo root
    dockerfilePath: Dockerfile

    # Render health check (your app must return HTTP 200 on this path)
    healthCheckPath: /

    autoDeploy: true

    envVars:
      - key: PYTHONUNBUFFERED
        value: "1"
