# ... (Line 3 is likely the start of the last envVar item)
- key: CELERY_RESULT_BACKEND
  fromService:
    type: keyValue
    name: celery-broker # <--- This line ends the fromService map
    # A list item ends here, but is not visually closed, causing resources: to be nested

resources: # <--- Error: Parser tries to nest this inside the previous list item
  # ...
