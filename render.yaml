# ... (Lines 1-36 remain the same, defining services, envVars, etc.)
# The preceding envVar section ends here (line 36):
#   - key: CELERY_RESULT_BACKEND
#     fromService:
#       type: keyValue
#       name: celery-broker

resources: # <-- THIS LINE MUST START at Column 1 (ZERO indentation)
  # 3. KEY-VALUE STORE (Redis for Celery Broker)
  - name: celery-broker
    type: keyValue
    plan: starter
    region: us-east
    maxmemoryPolicy: noeviction
