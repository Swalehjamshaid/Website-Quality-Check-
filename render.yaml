services:
  - type: web
    # ... web service configuration
    envVars: # <-- envVars list starts here
      - key: SOME_KEY
        value: SOME_VALUE
      - key: CELERY_RESULT_BACKEND # <-- This is the last item of the list
        fromService:
          type: keyValue
          name: celery-broker
    # The services block for this item ends here

resources: # <-- The top-level resources section must be here
  - name: celery-broker
    # ... etc.
