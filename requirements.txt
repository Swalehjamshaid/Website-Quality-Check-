# requirements.txt
# Check for and update any packages that are failing the install
Flask==2.3.3
gunicorn==21.2.0
python-dotenv==1.0.1
celery==5.3.6
# Add all your other specific dependencies here.
# Example problematic package fix:
# If 'psycopg2' fails, use the binary version:
# psycopg2-binary==2.9.9
