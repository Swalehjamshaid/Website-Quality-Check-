{
  "name": "rest-express-azay",
  "version": "1.0.0",
  "description": "Your project description here.",
  "main": "app.py",
  "scripts": {
    "start": "node server.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [
    "node",
    "express",
    "render"
  ],
  "author": "",
  "license": "ISC",
  "dependencies": {
    // Add your actual required packages here.
    // Ensure there is NO comma after the last entry in this list.
    // Example:
    // "express": "^4.18.2",
    // "dotenv": "^16.3.1"
  },
  "devDependencies": {
    // Add packages needed only for development/testing here.
  }
}
