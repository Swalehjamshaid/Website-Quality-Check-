{
  "name": "my-render-project",
  "version": "1.0.0",
  "description": "A Node.js application deployed to Render.",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [
    "node",
    "render",
    "express"
  ],
  "author": "Your Name Here",
  "license": "ISC",
  "dependencies": {
    // Add your production dependencies here, e.g.:
    // "express": "^4.18.2",
    // "dotenv": "^16.3.1"
  }
}
